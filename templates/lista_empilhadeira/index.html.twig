{% extends 'base.html.twig' %}

{% block title %}ListaEmpilhadeira index{% endblock %}

{% block body %}
    <h1>ListaEmpilhadeira index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for lista_empilhadeira in lista_empilhadeiras %}
            <tr>
                <td>{{ lista_empilhadeira.id }}</td>
                <td>
                    <a href="{{ path('lista_empilhadeira_show', {'id': lista_empilhadeira.id}) }}">show</a>
                    <a href="{{ path('lista_empilhadeira_edit', {'id': lista_empilhadeira.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="2">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('lista_empilhadeira_new') }}">Create new</a>
{% endblock %}
