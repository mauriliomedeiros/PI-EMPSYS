<form method="post" action="{{ path('lista_empilhadeira_delete', {'id': lista_empilhadeira.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ lista_empilhadeira.id) }}">
    <button class="btn">Delete</button>
</form>
