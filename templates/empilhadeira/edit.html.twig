{% extends 'base.html.twig' %}

{% block body %}
    <style>
        .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
        .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
    </style>
    <hr>
    <div class="row">
        <div class="col-md-12">
            <h4 class="text-center">EDITAR EMPILHADEIRA: {{ empilhadeira.fabricante }} {{ empilhadeira.modelo }}</h4>
        </div>
    </div>
    <hr>

    {{ include('empilhadeira/_form.html.twig', {'button_label': 'Update'}) }}

    <a class="btn btn-xs btn-primary pull-right" href="{{ path('app_empilhadeira_index') }}">back to list</a>

    {{ include('empilhadeira/_delete_form.html.twig') }}
{% endblock %}
